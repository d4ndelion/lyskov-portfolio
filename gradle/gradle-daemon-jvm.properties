# Gradle daemon JVM criteria (Gradle 8.8+ incubating feature).
# Gradle will use JDK 21 for the build daemon regardless of the system default.
# Kotlin 2.1.x does not yet support JDK 25 as a bytecode target; JDK 21 LTS is
# the correct compiler toolchain for this project.
toolchainVersion=21
